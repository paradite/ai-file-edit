{
  "name": "mcp-file-edit",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npm run build:all && jest",
    "test:watch": "npm run build:all && jest --watch",
    "build": "tsc && chmod 755 build/main.js",
    "build:server": "cd servers/filesystem && npm run build",
    "build:all": "npm run build && npm run build:server"
  },
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/paradite/mcp-file-edit.git"
  },
  "author": "paradite",
  "bugs": {
    "url": "https://github.com/paradite/mcp-file-edit/issues"
  },
  "homepage": "https://github.com/paradite/mcp-file-edit#readme",
  "description": "",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "@modelcontextprotocol/sdk": "^1.11.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.2",
    "typescript": "^5.8.3"
  }
}
